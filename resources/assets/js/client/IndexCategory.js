import React, { Component } from 'react';
import ReactDOM from 'react-dom';

import './IndexCategory.css';

export default class IndexCategory extends Component {

    render() {
        let display = <div/>;
        // check activated category and display the correct one
        // with the title
        return (
            <div>
                <div/**
                     * background of category
                     * buttons
                     */ />
                {display}
            </div>
        );
    }

}